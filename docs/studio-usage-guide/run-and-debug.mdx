---
sidebar_position: 4
---
import package_install from '@site/static/img/docs/package.png';
import terminal from '@site/static/img/docs/terminal.png';
import run_nodes from '@site/static/img/docs/run_nodes.png';
import run_btn from '@site/static/img/docs/run_btn.png';
import run_node from '@site/static/img/docs/run_node.png';
import flow_log from '@site/static/img/docs/flow_log.png';
import container_scripts_run from '@site/static/img/docs/container_scripts_run.png';
import bootstrap from '@site/static/img/docs/bootstrap.png';
import run_flow from '@site/static/img/docs/run_flow.png';

# ☕ Run and Debug

## 1. Install

### 1.1 Bootstrap Install Dependencies

> The `package.oo.yaml` file defines startup scripts to automate the bootstrap installation of project dependencies:
>
> 1. Node.js dependencies will be installed based on the package.json file
> 2. Python dependencies will be installed based on the requirements.txt file
> 3. System-level dependencies need to be manually added to the bootstrap script via apt-get

<img src={bootstrap} width="720" />

### 1.2 Developers Install Package

> 1. Installation through interface: Python packages will be automatically added to the requirements.txt file, Node.js packages will be automatically added to the package.json file
> 2. Installation through command line: Python packages need to be manually added to the requirements.txt file, while Node.js packages will automatically update package.json

<img src={package_install} width="720" />

## 2. Run 

### 2.1 Run Entire Flow

> Prerequisites: All dependencies are installed, and necessary parameters in the workflow are correctly configured

<img src={run_flow} width="720" />

### 2.2 Rerun some blocks in the flow

> Used for targeted debugging of selected multiple modules during the development process

<img src={run_nodes} width="720" />

### 2.3 Rerun a single block in the flow

> Used for precise debugging of a specific module during development

<img src={run_node} width="720" />

## 3. Log

### 3.1 Flow Log

> Main log output for development debugging, including console.log() output from Node.js code and print() output from Python code, among other information

<img src={flow_log} width="720" />

### 3.2 Container Bootstrap Log

> Records bootstrap information during container startup, primarily used for troubleshooting container initialization and environment configuration issues

<img src={container_scripts_run} width="720" />

