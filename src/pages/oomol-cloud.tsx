import React from "react";
import Layout from "../theme/Layout";
import styles from "./oomol-cloud.module.css";

export default function OomolCloud(): JSX.Element {
    return (
        <Layout>
            <div className={styles.box}>
                OomolCloud
            </div>
        </Layout>
    );
}
